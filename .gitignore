node_modules/

# Addons — not ready for release yet
addons/

# Compiled output (each package builds its own dist)
dist/

# OS
.DS_Store
Thumbs.db

# Internal process docs — local reference only, not part of the shipped project
BACKLOG.md
ECOSYSTEM_STATUS.md
**/HANDOVER.md
coding-standards/

# ai-dev-kit: classic single-session continuity file — local reference only
handover.md
# NOTE: handover/ (board.md, locks.md, roles/, PROTOCOL.md) is intentionally NOT
# ignored — it's the actual cross-session coordination mechanism for parallel
# mode and must be committed so other lanes/sessions can see claims and state.

# ai-dev-kit: installed skill copies — local tooling, not a project deliverable
.claude/skills/

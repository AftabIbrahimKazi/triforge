// Blender "Object" render mode alias → InstanceRenderer.
// Both names are exported so code may use either; they are the same class.
export { InstanceRenderer as ObjectRenderer } from './InstanceRenderer.js'
export type { InstanceRendererOptions as ObjectRendererOptions } from './InstanceRenderer.js'

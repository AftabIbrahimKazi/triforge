# st-curve-core benchmark — 2026-05-26

Node.js 24, Windows 11.

| Operation | µs/op | Notes |
|---|---|---|
| BezierCurve.getPoint (1 segment) | 0.4 | Hot path for single-segment eval |
| CatmullRomCurve.getPoint (5 points) | 0.3 | Per-point eval |
| CatmullRomCurve.getLength (200 divs) | 14.0 | Arc-length estimation |
| CatmullRomCurve.getSpacedPoints 64 | 25.9 | Uniform resampling |
| computeRMFrames (64 frames) | 61.7 | One-time frame precomputation |
| CurveTube.apply (64×12) | 140.1 | Geometry generation; call once, not per-frame |
| CurveLine.apply (128 pts) | 39.8 | Line geometry; call once |

## Notes

- `getPoint` at 0.3–0.4 µs: animating a path-follow object costs < 1 µs/frame.
- `computeRMFrames` at 62 µs: precomputed once in PathFollow constructor; never called per-frame.
- `CurveTube` at 140 µs: one-time geometry build. Not for use inside the render loop.
